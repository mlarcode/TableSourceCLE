@model IEnumerable<TableSourceCLE.Models.DonorCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.donorCategoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.donorCategoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.donorCategoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.donorCategoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.donorCategoryID })
        </td>
    </tr>
}

</table>
